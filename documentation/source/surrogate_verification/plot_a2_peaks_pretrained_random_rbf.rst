
.. DO NOT EDIT.
.. THIS FILE WAS AUTOMATICALLY GENERATED BY SPHINX-GALLERY.
.. TO MAKE CHANGES, EDIT THE SOURCE PYTHON FILE:
.. "surrogate_verification/plot_a2_peaks_pretrained_random_rbf.py"
.. LINE NUMBERS ARE GIVEN BELOW.

.. only:: html

    .. note::
        :class: sphx-glr-download-link-note

        :ref:`Go to the end <sphx_glr_download_surrogate_verification_plot_a2_peaks_pretrained_random_rbf.py>`
        to download the full example code.

.. rst-class:: sphx-glr-example-title

.. _sphx_glr_surrogate_verification_plot_a2_peaks_pretrained_random_rbf.py:


Paper Peaks Verification: Pretrained Random-Sampling RBF Surrogate
==================================================================

This example verifies a pretrained radial basis function (RBF) surrogate on the
2D Peaks benchmark function used by :cite:`voronoi_adaptive_surrogates`. This example is 
part of the verification example set for surrogates. This set of examples is
meant to demonstrate features and behavior of the surrogates in MatCal. 
They are not meant to be used as templates for user MatCal files.

This is a **pretrained** surrogate example:

* all training samples are chosen before surrogate construction;
* samples are independent uniform random samples over the Peaks domain;
* a separate RBF surrogate is trained at each requested sample count;
* all surrogates are scored on the same fixed validation set.

The example produces two figures.

1. A convergence plot of validation RMSE and validation maximum absolute error
   versus number of training samples. This is the Sphinx-Gallery thumbnail.
2. Diagnostic plots at 50, 100, and 150 samples showing the true Peaks function
   and the absolute surrogate error field with training samples overlaid.

The empirical convergence rate is estimated by fitting

.. math::

    E(N) \approx C N^{-p}.

.. GENERATED FROM PYTHON SOURCE LINES 31-35

.. code-block:: Python


    # The convergence plot is the first figure created by this example.
    # sphinx_gallery_thumbnail_number = 1








.. GENERATED FROM PYTHON SOURCE LINES 36-38

Imports
-------

.. GENERATED FROM PYTHON SOURCE LINES 38-63

.. code-block:: Python


    import os

    import matplotlib.pyplot as plt
    import numpy as np

    import matcal as mc

    from includes.paper_peaks_verification_common import (
        INDEPENDENT_FIELD,
        INDEPENDENT_VALUES,
        RANDOM_SEED,
        TARGET_FIELD,
        get_field_score,
        make_model,
        make_objective,
        make_parameters,
        make_uniform_random_samples,
        plot_convergence_history,
        plot_function_and_surrogate_error_at_counts,
        print_convergence_summary,
        run_fixed_validation_set,
        run_parameter_study_for_samples,
    )





.. rst-class:: sphx-glr-script-out

 .. code-block:: none

    /gpfs/knkarls/projects/matcal_devel/external_matcal/documentation/surrogate_verification/includes/paper_surrogate_verification_common.py:942: SyntaxWarning: invalid escape sequence '\h'
      |f(x) - \hat{f}(x)|,




.. GENERATED FROM PYTHON SOURCE LINES 64-66

User options
------------

.. GENERATED FROM PYTHON SOURCE LINES 66-84

.. code-block:: Python


    MAX_TRAINING_SAMPLES = 150
    SAMPLE_COUNTS_TO_PLOT = (50, 100, 150)

    WORKING_DIRECTORY = os.path.abspath("paper_peaks_pretrained_random_rbf")
    FIGURE_DIRECTORY = os.path.join(WORKING_DIRECTORY, "figures")

    SAMPLE_COUNTS = tuple(
        sorted(set(range(20, MAX_TRAINING_SAMPLES + 1, 20)) | set(SAMPLE_COUNTS_TO_PLOT))
    )

    RBF_SURROGATE_OPTIONS = {
        "regressor_type": "RBF",
        "neighbors": 50,
        "decomp_var": 0.99,
    }









.. GENERATED FROM PYTHON SOURCE LINES 85-87

Training helper
---------------

.. GENERATED FROM PYTHON SOURCE LINES 87-143

.. code-block:: Python


    def train_and_score_random_rbf(
        parameters,
        model,
        objective,
        validation_results,
        samples,
        sample_count,
    ):
        """
        Train and score one pretrained random-sampling RBF surrogate.
        """
        training_samples = samples[:sample_count]

        training_directory = os.path.join(
            WORKING_DIRECTORY,
            "random_training",
            f"n_{int(sample_count)}",
        )

        training_results = run_parameter_study_for_samples(
            parameters,
            model,
            objective,
            training_samples,
            training_directory,
        )

        surrogate_generator = mc.SurrogateGenerator(
            training_results,
            interpolation_field=INDEPENDENT_FIELD,
            interpolation_locations=INDEPENDENT_VALUES,
            training_fraction=1.0,
            test_eval_info=validation_results,
            regressor_type=RBF_SURROGATE_OPTIONS["regressor_type"],
            neighbors=RBF_SURROGATE_OPTIONS["neighbors"],
        )

        surrogate_generator.set_fields_of_interest(TARGET_FIELD)
        surrogate_generator.set_PCA_details(
            decomp_var=RBF_SURROGATE_OPTIONS["decomp_var"],
        )

        save_name = os.path.join(
            training_directory,
            f"random_rbf_surrogate_{int(sample_count)}",
        )

        surrogate = surrogate_generator.generate(save_name)

        rmse = get_field_score(surrogate.rmse_errors["test"], TARGET_FIELD)
        max_error = get_field_score(surrogate.max_errors["test"], TARGET_FIELD)

        return surrogate, rmse, max_error









.. GENERATED FROM PYTHON SOURCE LINES 144-146

Create output directories.
--------------------------

.. GENERATED FROM PYTHON SOURCE LINES 146-151

.. code-block:: Python


    os.makedirs(WORKING_DIRECTORY, exist_ok=True)
    os.makedirs(FIGURE_DIRECTORY, exist_ok=True)









.. GENERATED FROM PYTHON SOURCE LINES 152-160

Create the MatCal model components.
-----------------------------------

The helper functions create:

* two bounded parameters, ``x1`` and ``x2``;
* a MatCal ``PythonModel`` wrapping the Peaks function;
* a ``SimulationResultsSynchronizer`` for the scalar one-point response.

.. GENERATED FROM PYTHON SOURCE LINES 160-166

.. code-block:: Python


    parameters = make_parameters()
    model = make_model()
    objective = make_objective()









.. GENERATED FROM PYTHON SOURCE LINES 167-172

Generate one fixed validation set.
----------------------------------

Every surrogate is scored on the same validation set so that differences in
error are due to training sample count and not due to changing test data.

.. GENERATED FROM PYTHON SOURCE LINES 172-182

.. code-block:: Python


    print("Generating fixed validation set.")
    validation_results = run_fixed_validation_set(
        parameters,
        model,
        objective,
        os.path.join("fixed_validation_set"),
    )






.. rst-class:: sphx-glr-script-out

 .. code-block:: none

    Generating fixed validation set.
    Loading cached fixed validation set from:
      /gpfs/knkarls/projects/matcal_devel/external_matcal/documentation/surrogate_verification/fixed_validation_set/final_results.joblib




.. GENERATED FROM PYTHON SOURCE LINES 183-188

Generate nested random training samples.
----------------------------------------

The 100-sample surrogate uses the first 100 samples of the same random sample
sequence used by the 150-sample surrogate.

.. GENERATED FROM PYTHON SOURCE LINES 188-196

.. code-block:: Python


    print(f"Generating {MAX_TRAINING_SAMPLES} nested random training samples.")
    random_samples = make_uniform_random_samples(
        MAX_TRAINING_SAMPLES,
        seed=RANDOM_SEED,
    )






.. rst-class:: sphx-glr-script-out

 .. code-block:: none

    Generating 150 nested random training samples.




.. GENERATED FROM PYTHON SOURCE LINES 197-202

Train and score the pretrained RBF surrogates.
----------------------------------------------

``surrogates_by_count`` and ``training_samples_by_count`` are also populated
so the 50, 100, and 150 sample diagnostic error-field plots can be made later.

.. GENERATED FROM PYTHON SOURCE LINES 202-239

.. code-block:: Python


    rmse_history = []
    max_error_history = []

    surrogates_by_count = {}
    training_samples_by_count = {}

    for sample_count in SAMPLE_COUNTS:
        print(f"\nTraining pretrained random-sampling RBF with {sample_count} samples.")

        surrogate, rmse, max_error = train_and_score_random_rbf(
            parameters,
            model,
            objective,
            validation_results,
            random_samples,
            sample_count,
        )

        rmse_history.append(rmse)
        max_error_history.append(max_error)

        if sample_count in SAMPLE_COUNTS_TO_PLOT:
            surrogates_by_count[int(sample_count)] = {
                "surrogate": surrogate,
                "surrogate_index": None,
            }
            training_samples_by_count[int(sample_count)] = random_samples[:sample_count]

        print(f"  validation RMSE:               {rmse:.6e}")
        print(f"  validation max absolute error: {max_error:.6e}")

    sample_counts = np.asarray(SAMPLE_COUNTS, dtype=int)
    rmse_history = np.asarray(rmse_history, dtype=float)
    max_error_history = np.asarray(max_error_history, dtype=float)






.. rst-class:: sphx-glr-script-out

 .. code-block:: none


    Training pretrained random-sampling RBF with 20 samples.
      validation RMSE:               1.216972e+00
      validation max absolute error: 6.778664e+00

    Training pretrained random-sampling RBF with 40 samples.
      validation RMSE:               9.963003e-01
      validation max absolute error: 6.776797e+00

    Training pretrained random-sampling RBF with 50 samples.
      validation RMSE:               9.875689e-01
      validation max absolute error: 6.768290e+00

    Training pretrained random-sampling RBF with 60 samples.
      validation RMSE:               9.668153e-01
      validation max absolute error: 6.940313e+00

    Training pretrained random-sampling RBF with 80 samples.
      validation RMSE:               8.064164e-01
      validation max absolute error: 6.016851e+00

    Training pretrained random-sampling RBF with 100 samples.
      validation RMSE:               7.499807e-01
      validation max absolute error: 5.960962e+00

    Training pretrained random-sampling RBF with 120 samples.
      validation RMSE:               6.980532e-01
      validation max absolute error: 5.493431e+00

    Training pretrained random-sampling RBF with 140 samples.
      validation RMSE:               6.368715e-01
      validation max absolute error: 5.025806e+00

    Training pretrained random-sampling RBF with 150 samples.
      validation RMSE:               5.606465e-01
      validation max absolute error: 4.819553e+00




.. GENERATED FROM PYTHON SOURCE LINES 240-242

Report convergence statistics.
------------------------------

.. GENERATED FROM PYTHON SOURCE LINES 242-251

.. code-block:: Python


    print_convergence_summary(
        "pretrained random-sampling RBF",
        sample_counts,
        rmse_history,
        max_error_history,
    )






.. rst-class:: sphx-glr-script-out

 .. code-block:: none


    pretrained random-sampling RBF convergence summary:
      final sample count:         150
      final RMSE:                 5.606465e-01
      final max absolute error:   4.819553e+00
      best max absolute error:    4.819553e+00
      best max-error sample count:150
      estimated RMSE rate p:      0.3639
      estimated max-error rate p: 0.1775




.. GENERATED FROM PYTHON SOURCE LINES 252-256

Plot convergence versus number of training samples.
---------------------------------------------------

This is figure 1 and is used as the Sphinx-Gallery thumbnail.

.. GENERATED FROM PYTHON SOURCE LINES 256-267

.. code-block:: Python


    plot_convergence_history(
        sample_counts,
        rmse_history,
        max_error_history,
        method_name="pretrained random-sampling RBF",
        figure_directory=FIGURE_DIRECTORY,
        filename="peaks_pretrained_random_rbf_convergence.png",
    )





.. image-sg:: /surrogate_verification/images/sphx_glr_plot_a2_peaks_pretrained_random_rbf_001.png
   :alt: Peaks verification: pretrained random-sampling RBF
   :srcset: /surrogate_verification/images/sphx_glr_plot_a2_peaks_pretrained_random_rbf_001.png
   :class: sphx-glr-single-img


.. rst-class:: sphx-glr-script-out

 .. code-block:: none


    (<Figure size 750x520 with 1 Axes>, <Axes: title={'center': 'Peaks verification: pretrained random-sampling RBF'}, xlabel='number of training samples', ylabel='validation error'>)



.. GENERATED FROM PYTHON SOURCE LINES 268-273

Plot true function and surrogate error fields.
----------------------------------------------

The first row shows the true Peaks function with samples overlaid. The second
row shows the absolute surrogate error field for the corresponding surrogate.

.. GENERATED FROM PYTHON SOURCE LINES 273-285

.. code-block:: Python


    plot_function_and_surrogate_error_at_counts(
        surrogates_by_count,
        training_samples_by_count,
        sample_counts_to_plot=SAMPLE_COUNTS_TO_PLOT,
        figure_directory=FIGURE_DIRECTORY,
        filename="peaks_pretrained_random_rbf_error_fields.png",
        method_name="pretrained random-sampling RBF",
        n_grid=150,
    )

    print(f"\nFigures saved to: {FIGURE_DIRECTORY}")
    plt.show()


.. image-sg:: /surrogate_verification/images/sphx_glr_plot_a2_peaks_pretrained_random_rbf_002.png
   :alt: Peaks verification: pretrained random-sampling RBF, True Peaks function N=50, True Peaks function N=100, True Peaks function N=150, Absolute surrogate error N=50, field max=6.794e+00, Absolute surrogate error N=100, field max=5.962e+00, Absolute surrogate error N=150, field max=4.978e+00
   :srcset: /surrogate_verification/images/sphx_glr_plot_a2_peaks_pretrained_random_rbf_002.png
   :class: sphx-glr-single-img


.. rst-class:: sphx-glr-script-out

 .. code-block:: none


    Figures saved to: /gpfs/knkarls/projects/matcal_devel/external_matcal/documentation/surrogate_verification/paper_peaks_pretrained_random_rbf/figures





.. rst-class:: sphx-glr-timing

   **Total running time of the script:** (1 minutes 26.238 seconds)


.. _sphx_glr_download_surrogate_verification_plot_a2_peaks_pretrained_random_rbf.py:

.. only:: html

  .. container:: sphx-glr-footer sphx-glr-footer-example

    .. container:: sphx-glr-download sphx-glr-download-jupyter

      :download:`Download Jupyter notebook: plot_a2_peaks_pretrained_random_rbf.ipynb <plot_a2_peaks_pretrained_random_rbf.ipynb>`

    .. container:: sphx-glr-download sphx-glr-download-python

      :download:`Download Python source code: plot_a2_peaks_pretrained_random_rbf.py <plot_a2_peaks_pretrained_random_rbf.py>`

    .. container:: sphx-glr-download sphx-glr-download-zip

      :download:`Download zipped: plot_a2_peaks_pretrained_random_rbf.zip <plot_a2_peaks_pretrained_random_rbf.zip>`


.. only:: html

 .. rst-class:: sphx-glr-signature

    `Gallery generated by Sphinx-Gallery <https://sphinx-gallery.github.io>`_
