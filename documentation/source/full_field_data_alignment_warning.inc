.. warning::
    MatCal currently does not attempt to align full-field data 
    from two different objects before operating on it. The 
    user must ensure that the data are properly aligned 
    in two-dimensional space before using MatCal's tool 
    for full-field mapping or comparisons.